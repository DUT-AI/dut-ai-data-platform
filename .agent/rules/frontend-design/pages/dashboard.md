# Dashboard Rules

Purpose: help the signed-in user decide what to do next. Order: a visually distinct welcome region with today's workload, attention queue, assigned projects and roles, personal throughput/quality, then recent activity. A regional tint may separate the welcome area, but content hierarchy and contrast remain primary. Global platform totals belong to admin reporting, not the primary dashboard.
