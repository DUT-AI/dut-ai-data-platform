# Dataset Rules

Represent Dataset → immutable or lifecycle-controlled Dataset Version → Asset. This tab is a data browser, comparable to opening a folder: users inspect versions, folders/assets, previews, metadata, and provenance. Annotation is a secondary action on an asset, never the page's primary identity. Make the active version, origin/inheritance, status, import progress, asset counts, filters, permissions, and preview action explicit.
