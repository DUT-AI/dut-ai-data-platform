# Projects Rules

Purpose: find and compare workspaces. Each project exposes problem type, modality/template, user role, lifecycle status, annotation progress, active dataset/ontology versions, last update, and one primary action. Provide search, filter, sort, loading, empty, and permission states.
